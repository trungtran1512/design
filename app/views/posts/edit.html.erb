<!DOCTYPE html>
<html>
<meta name="viewport" content="width=device-width, initial-scale=1">
<link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
<style>
body { font-family: "Times New Roman", Georgia, Serif; }
h1, h2, h4, h5, h6 {
  font-family: "Playfair Display";
  letter-spacing: 5px;
}
h3 {
  letter-spacing: 2px;
}
</style>
<head>
</head>
  <body>
    <!-- header -->
    <nav class="navbar navbar-expand-lg navbar-light" id="navbar_user">
      <div class="container">
        <div class="logo-item">
          <%= image_tag("l.png", alt: "logo_design", style: "height: 60px;") %>
        </div>
      </div>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <%= content_tag(:i, "blur_on", class: "material-icons", style: "color: white;") %>
      </button>
      <div class="collapse navbar-collapse nav-collapse" id="navbarNavDropdown">
        <div class="main-container">
          <ul class="navbar-nav">
            <li class="nav-item active">
              <%= link_to "Home", root_path, class: "nav-link" %>
            </li>
            <li class="nav-item active">
              <%= link_to "Submit a photo", new_post_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <a href="#posts" class="nav-link">Photo by me</a>
            </li>
            <li class="nav-item">
              <% if user_signed_in? %>
              <%= link_to('Logout', destroy_user_session_path, method: :delete, class: "nav-link") %>
              <% else %>
                <%= link_to "Login", new_user_session_path, class: "nav-link" %>
                <% end %>
            </li>
            <li class="nav-item" id="btn-join">
              <% if !user_signed_in? %>
                <%= link_to "Join Free", new_user_registration_path, class: "nav-link" %>
              <% else %>
                <%= link_to "#{current_user.username}", users_path, class: "nav-link" %>
              <% end %>
            </li>
          </ul>
        </div>
      </div>
    </nav>
    <br>
    <div class="w3-content" style="max-width:1100px">
      <div class="w3-row w3-padding-64" id="profile" style="border-radius: 100%;">
       <h5 class="card-header info-color white-text text-center py-4">
        <strong style="color: black;"> Edit Post </strong>
          <p style="text-align: center; opacity: 0.4; font-family: cursive;"></p>
          <%= link_to '#homepage', root_path, class: 'btn btn-outline-dark' %>
        </h5>
        <%= simple_form_for @post do |f| %>
          <div class="card-body px-lg-5 pt-0">
            <form class="text-center" style="color: #757575;">
              <% if @post.errors.present? %>
                <br><%= content_tag :div, @post.errors.full_messages.join('<br>').html_safe, class: "alert alert-danger", style: "text-align: center;" %>
              <% end %>

              <div class="form-group">
                <label for="Published" class="bmd-label-floating"></label>
                <%= f.input :published, as: :radio_buttons %>
              </div>

              <div class="form-group">
                <label for="Image" class="bmd-label-floating"></label>
                <%= f.input :image, as: :file, label: 'Upload Image' %>
              </div>

              <div class="form-group">
                <label for="Title" class="bmd-label-floating">Title</label>
                <%= f.input :title, label: false, required: true, autocomplete: "title", input_html: { class: "form-control" } %>
                <span class="bmd-help">We'll never share your title with anyone else.</span>
              </div>

              <div class="form-group">
                <label for="Discription" class="bmd-label-floating">Discription</label>
                <%= f.input :discription, label: false, required: true, autocomplete: "username", input_html: { class: "form-control" } %>
                <span class="bmd-help">We'll never share your discription with anyone else.</span>
              </div>

              <div class="col-md-4 offset-md-4">
                <%= f.button :submit, "Update", class: "btn btn-raised btn-info btn-rounded btn-block my-4 waves-effect z-depth-0" %>
                <%= link_to "Back", posts_path, class: "btn btn-raised btn-info btn-rounded btn-block my-4 waves-effect z-depth-0" %>
              </div>
            </form>
          </div>
        <% end %>
      </div>
    </div>
  </body>
</html>